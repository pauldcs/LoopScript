loop
	1   ; const RENAME (1042, 1027), (485, 489), 0.5;
		mov $RENAME ;
			click left ;
			del 16
		; const BAR (916, 945), (438, 450), 0.5;
		mov $BAR ;
			click left ;
			rtxt (10, 16)
		; const APPLY (1029, 1090), (654, 661), 0.4;
		mov $APPLY ;
			click left
		;
	end
;